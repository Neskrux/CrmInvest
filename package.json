{
  "name": "crm-system",
  "version": "1.0.0",
  "description": "CRM System with Frontend and Backend",
  "scripts": {
    "build": "cd frontend && npm install --force && npx react-scripts build",
    "install": "cd frontend && npm install --force && cd ../backend && npm install",
    "start": "node backend/server.js",
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=14.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.55.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.1"
  }
}
